# React Diary App (QA Portfolio Project)

## 📌 프로젝트 개요

React를 활용해 CRUD 기능을 갖춘 일기장 웹 애플리케이션입니다.
본 프로젝트는 **기능 구현 자체보다 QA 관점에서의 테스트, 버그 분석, 재현 과정 정리**에 초점을 두고 진행되었습니다.

---

## 🛠 기술 스택

- React (Vite)
- React Router
- Context API / useReducer
- JavaScript (ES6)
- CSS

---

## ✨ 주요 기능

- 일기 작성 / 수정 / 삭제 (CRUD)
- 날짜 기반 일기 관리
- 감정 선택 기능
- 페이지 라우팅
  - Home
  - New
  - Diary
  - Edit
  - NotFound

---

## 🔍 QA 관점 테스트 포인트

### 1. 라우팅 관련

- 존재하지 않는 ID로 접근 시 처리 여부
- 새로고침 시 상태 유지 여부
- 뒤로가기 / replace 옵션 동작 확인

### 2. 상태 관리

- Context 상태 변경 시 렌더링 정상 여부
- 삭제 후 잘못된 데이터 접근 방지
- id 타입(string / number) 불일치로 인한 오류 가능성

### 3. UX / 예외 처리

- 잘못된 접근 시 alert 노출
- 삭제 확인(confirm) UX
- 빈 데이터 상태에서 화면 표시 여부

---

## 🐞 실제 발생했던 버그 사례

### 🔴 버그 1: 수정 페이지 접근 시 "존재하지 않는 일기입니다" alert 발생

**재현 조건**

1. Home → Diary → 수정하기 버튼 클릭
2. 즉시 alert 발생 후 메인 페이지로 이동

**원인**

- URL param의 id는 string
- 상태 데이터의 id는 number
- strict equality 비교(`===`)로 인해 매칭 실패

**해결**

```js
item.id === Number(params.id);
```

## 🧪 QA Bug Report #1

### 🔴 Bug Title

일기 수정 페이지에서 수정 완료 시 “존재하지 않는 일기입니다” 경고 발생

---

### 📌 Environment

- OS: Windows 11
- Browser: Chrome
- Framework: React
- Routing: React Router v6

---

### 🔁 Steps to Reproduce

1. 메인 페이지에서 기존 일기 하나를 선택한다
2. **[수정하기] 버튼 클릭**
3. 일기 내용을 수정한 후 **[작성완료] 버튼 클릭**
4. 확인 팝업에서 **[확인]** 선택

---

### ❗ Expected Result

- 수정된 일기가 정상적으로 저장되고
- 메인 페이지로 이동한다

---

### ❌ Actual Result

- “존재하지 않는 일기입니다” 알림이 표시되며
- 수정된 일기가 사라짐

---

### 🔍 Root Cause Analysis

- URL 파라미터(`params.id`)는 **문자열(string)** 타입
- Diary 데이터의 `id`는 **숫자(number)** 타입
- `Array.find()`에서 타입 불일치로 인해 일기 데이터를 찾지 못함

````js
data.find((item) => item.id === params.id); // ❌ 항상 false

🛠 Fix

URL 파라미터를 숫자로 변환하여 비교하도록 수정

```md
```js
const currentDiaryItem = data.find(
(item) => item.id === Number(params.id)
);

onUpdate, onDelete에서도 동일하게 타입 통일

✅ Result After Fix

수정 후 정상적으로 메인 페이지 이동

수정된 일기 데이터 유지

경고 메시지 발생하지 않음

🧾 Related Commits

test: reproduce diary edit error caused by id type mismatch

fix: resolve diary edit page error by converting params id to number

🎯 QA Perspective

이 프로젝트를 통해 다음과 같은 QA 역량을 실습했습니다:

사용자 시나리오 기반 버그 재현

데이터 타입 불일치로 인한 오류 분석

수정 전/후 동작 비교

Git 커밋을 통한 이력 관리

개발자와 협업 가능한 수준의 버그 리포트 작성

````

```

```
